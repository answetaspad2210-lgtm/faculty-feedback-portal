# client/.env.example
# Copy to client/.env if you need to override the API base URL.
# By default the Vite dev server proxies /api to the backend (see vite.config.js),
# so this is usually not required for local development.
VITE_API_BASE_URL=/api
